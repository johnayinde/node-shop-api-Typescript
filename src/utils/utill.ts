// import { Model } from 'mongoose';

// const documentExist = async (id, model) => {
//     try {
//         const exist = await model.exists({ _id: id });

//         if (!exist) return new Error('Product does not exist');
//         return exist;
//     } catch (error) {}
// };

// export { documentExist };
